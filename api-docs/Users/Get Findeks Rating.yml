info:
  name: Get Findeks Rating
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/api/users/getuserfindeksrating?findeksRating={{findeksRating}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
