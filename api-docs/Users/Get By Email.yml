info:
  name: Get By Email
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/users/getbymail?email={{email}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
