info:
  name: Get By Id
  type: http
  seq: 6

http:
  method: GET
  url: '{{baseUrl}}/api/users/getuserbyid?id={{userId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
