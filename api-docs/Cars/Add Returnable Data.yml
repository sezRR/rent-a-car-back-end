info:
  name: Add Returnable Data
  type: http
  seq: 8

http:
  method: POST
  url: '{{baseUrl}}/api/cars/addreturnabledata'
  body:
    type: json
    data: |
      {
        "brandId": {{brandId}},
        "colorId": {{colorId}},
        "modelYear": 2026,
        "dailyPrice": 125.00,
        "description": "Example rental car",
        "minimumFindeksRating": 1000
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
