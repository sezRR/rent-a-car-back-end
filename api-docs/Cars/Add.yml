info:
  name: Add
  type: http
  seq: 7

http:
  method: POST
  url: '{{baseUrl}}/api/cars/add'
  body:
    type: json
    data: |
      {
        "brandId": {{brandId}},
        "colorId": {{colorId}},
        "modelYear": 2026,
        "dailyPrice": 125.00,
        "description": "Example rental car",
        "minimumFindeksRating": 1000
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
