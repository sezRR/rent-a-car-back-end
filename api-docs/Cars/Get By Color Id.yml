info:
  name: Get By Color Id
  type: http
  seq: 5

http:
  method: GET
  url: '{{baseUrl}}/api/cars/getcarsbycolorid?colorId={{colorId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
