info:
  name: Delete
  type: http
  seq: 5

http:
  method: POST
  url: '{{baseUrl}}/api/brands/delete'
  body:
    type: json
    data: |
      {
        "id": {{brandId}},
        "brandName": "Volvo"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
