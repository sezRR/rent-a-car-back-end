info:
  name: Update
  type: http
  seq: 4

http:
  method: POST
  url: '{{baseUrl}}/api/brands/update'
  body:
    type: json
    data: |
      {
        "id": {{brandId}},
        "brandName": "Volvo"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
