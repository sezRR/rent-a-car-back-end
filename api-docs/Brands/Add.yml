info:
  name: Add
  type: http
  seq: 3

http:
  method: POST
  url: '{{baseUrl}}/api/brands/add'
  body:
    type: json
    data: |
      {
        "brandName": "Volvo"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
