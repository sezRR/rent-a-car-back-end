info:
  name: Get By User Id
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/customers/getcustomersbyuserid?userId={{userId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
