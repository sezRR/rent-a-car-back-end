info:
  name: Get By Id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/getuserbyid?id={{userId}}"
  params:
    - name: id
      value: "{{userId}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
