info:
  name: Get Findeks Rating
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/getuserfindeksrating?findeksRating={{findeksRating}}"
  params:
    - name: findeksRating
      value: "{{findeksRating}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
