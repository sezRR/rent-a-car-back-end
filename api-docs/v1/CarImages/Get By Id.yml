info:
  name: Get By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/carimages/getbyid?id={{carImageId}}"
  params:
    - name: id
      value: "{{carImageId}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
