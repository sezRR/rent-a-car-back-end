info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/carimages/delete?id={{carId}}"
  params:
    - name: id
      value: "{{carId}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
