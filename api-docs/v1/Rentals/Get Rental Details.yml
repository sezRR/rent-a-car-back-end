info:
  name: Get Rental Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/rentals/getrentaldetails"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
