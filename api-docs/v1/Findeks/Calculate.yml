info:
  name: Calculate
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/findeks/calculate"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
