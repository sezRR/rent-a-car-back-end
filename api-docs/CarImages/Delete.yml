info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: '{{baseUrl}}/api/carimages/delete?id={{carId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
