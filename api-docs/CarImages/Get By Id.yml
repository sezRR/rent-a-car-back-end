info:
  name: Get By Id
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/carimages/getbyid?id={{carImageId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
