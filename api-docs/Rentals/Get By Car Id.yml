info:
  name: Get By Car Id
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/rentals/getrentalbycarid?carId={{carId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
