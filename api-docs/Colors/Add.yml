info:
  name: Add
  type: http
  seq: 3

http:
  method: POST
  url: '{{baseUrl}}/api/colors/add'
  body:
    type: json
    data: |
      {
        "colorName": "Blue"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
