info:
  name: Get By Id
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/colors/getbyid?id={{colorId}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
