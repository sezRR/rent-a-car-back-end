info:
  name: Delete
  type: http
  seq: 5

http:
  method: POST
  url: '{{baseUrl}}/api/colors/delete'
  body:
    type: json
    data: |
      {
        "id": {{colorId}},
        "colorName": "Blue"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
