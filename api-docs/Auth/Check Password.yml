info:
  name: Check Password
  type: http
  seq: 2

http:
  method: POST
  url: '{{baseUrl}}/api/auth/checkpassword'
  body:
    type: json
    data: |
      {
        "email": "{{email}}",
        "password": "Password123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
